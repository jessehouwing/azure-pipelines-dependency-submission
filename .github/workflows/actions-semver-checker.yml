name: Check SemVer

on:
  push:
    branches: ['main']
    tags:
      - '*'
  workflow_dispatch:

permissions: {}

jobs:
  check-semver:
    concurrency:
      group: '${{ github.workflow }}'
      cancel-in-progress: true
    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      - uses: jessehouwing/actions-semver-checker@6d03f353bc99898ba501da95fb2c866a9946a75e # v2.0.4
        with:
          check-minor-version: error
          check-release-immutability: error
          ignore-preview-releases: true
          check-releases: error
          token: ${{ secrets.GITHUB_TOKEN }}
          auto-fix: true
